<% if user_signed_in? %>
  <li>
  <%= link_to('Settings', edit_user_registration_path, :class => "admin-link opacity") %>
  </li>
<% else %>
  <li>
  <%= link_to('Sign up', new_user_registration_path, :class => "admin-link opacity")  %>
  </li>
<% end %>
