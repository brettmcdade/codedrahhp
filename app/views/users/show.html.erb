<div id="container"> 
<!--<img class="loader" src="loader.png" />
-->        
	<div class="box">
    	<div class="content">
            <div class="inner">
            	
				<%= form_for([@user, current_user.mediafiles.build]) do |f| %>
				  <div>
					<%= f.file_field :mp3 %>
				  </div>
				  <div class="actions">
				    <%= f.submit "Upload" %>
				  </div>
				<% end %>
					
                </div>
            </div>
        </div>
</div>
